name: "[schedule-parser v2] Parse Schedule and Upload to R2"

on:
  workflow_dispatch:
    inputs:
      duToken:
        required: true
        description: "DU auth bearer token"
        type: string

permissions:
  actions: write

jobs:
  parse-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup PHP with PECL extension
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: iconv, json, zlib
          tools: composer

      - name: Install PHP dependencies
        run: |
          cd tools/schedule-parser/v2
          composer install

      - name: Create output directory
        run: mkdir -p ./output

      - name: Parse schedule files
        run: |
          cd tools/schedule-parser/v2
          php command.php ${{ inputs.duToken }} ${{ github.workspace }}/external/aitu/schedule ${{ github.workspace }}/output/main.json

      - name: List generated files
        run: |
          echo "Generated files:"
          ls -la ./output/ || echo "No output files found"

      - name: Setup rclone
        uses: AnimMouse/setup-rclone@v1
        with:
          rclone_config: ${{ secrets.RCLONE_CONFIG }}

      - name: Upload to R2 bucket
        run: |
          echo "Uploading parsed schedule files to R2..."

          rclone sync ./output remoodle:calendar-schedule -v

      - name: Trigger cabgen workflow
        run: gh workflow run cabgen-upload.yml --ref ${{ github.ref_name }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
